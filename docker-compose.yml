version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    container_name: tewi-db
    environment:
      POSTGRES_DB: tewi
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d tewi"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: tewi-app
    environment:
      DATABASE_URL: postgres://user:password@postgres:5432/tewi
      RUST_LOG: debug
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
